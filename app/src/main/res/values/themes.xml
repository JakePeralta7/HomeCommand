<resources>
    <style name="Theme.HomeCommand" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Activity transitions: shared-axis pushes between main and sub-screens. -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>

        <!-- App-local mirror of a library attr so code can stay on the non-transitive app R. -->
        <item name="touchSelectBackground">?android:attr/selectableItemBackground</item>
    </style>
</resources>
